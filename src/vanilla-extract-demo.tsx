import { Button } from '@mantine/core'
import { demo } from './vanila-extract-demo.css';

export const VanillaExtractDemo: React.FC = () => {
  return <Button className={demo}>Sample Button</Button>
}